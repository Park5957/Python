문서 07HW20220210박현용18의 일지.
[26] 문항 피보나치 수열

수열 작성에 필요한 요소

	1) 수열을 입력할 빈 리스트
	2) 입력값을 계산 기동할 함수
	3) 입력 정수를 최대 값으로 돌리는 for문
	3) 계산된 수열을 입력할 변수

c = []
a = b, b=b+a 로 지정
for문 실행. 
입력값 n을 최대치로 갖는 for문의 반복으로 인해
피보나치 수열이 아닌 마구잡이 계산이 발생.

최대값 n 을 벗어나서 배열이 생성됨을 확인.
if b<=n 이 잘못 되었음을 확인
if a <= n 으로 변경.

for문 재 실행
피보나치 수열이 아닌 마구잡이 계산이 발생.
확인결과 입력값의 왼쪽 첫번째 두번째 배열의 값으로 계산하는 것이 아닌 잘못된 증식으로 계산됨을 확인

추가
리스트에 입력된 숫자가 n값을 초과한 값으로 입력됨을 확인
range(n) 의 모든 반복을 c 입력 시키지 않을 조정이 필요
c에 입력변수로 지정한 a,b를 for문 밖으로 빼냄
최초 값인 0,1만 지정한 변수로 내버려둔 뒤
if 문 안에서 계산 과 입력을 하기로 함.

재실행.
동일한 문제 발생.
계산식을 변경 하기로 함.
if 문 안에서 b를 완전 제거
a만 사용하기로 함.

계산식을 a = c[i] + c[i+1] 로 변경
실행 계산식은 맞으나 n의 값을 벗어나 c 에 입력됨을 확인

if 문내 추가 if 생성
if a<= n : c.append(a) 로 진행하기로 함.

재실행.
정상작동 확인.





